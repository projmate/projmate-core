// Generated by CoffeeScript 1.6.1
(function() {
  var fs, path, str;

  fs = require("fs");

  path = require("path");

  str = require("underscore.string");

  module.exports = {
    getResource: function(element) {
      return element.match(/(href|src)='([^']+)'/)[2];
    },
    fileContains: function(filename, pattern) {
      var content;
      filename = path.join(__dirname, filename);
      content = fs.readFileSync(filename, 'utf8');
      return !!content.match(pattern);
    },
    isSingleLine: function(filename) {
      var content;
      filename = path.join(__dirname, filename);
      content = fs.readFileSync(filename, 'utf8');
      return str.count(content, "\n") === 0;
    }
  };

}).call(this);
